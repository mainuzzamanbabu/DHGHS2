<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dhghs">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminRegisterStudent"></activity>
        <activity android:name=".SeeRegisteredStudent" />
        <activity android:name=".MoreDetails" />
        <activity android:name=".AdminDashboard" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".RegisterForProgram" />
        <activity android:name=".userProgramActivity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>